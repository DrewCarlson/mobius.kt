apply plugin: 'kotlin-platform-native'

components.main {
  baseName = 'Todo'
  targets = ['ios_x64']
  outputKinds = [FRAMEWORK]
  dependencies {
    implementation project(":native:core-native")
  }
}

dependencies {
  expectedBy project(":samples:todo:todo-common")
}
